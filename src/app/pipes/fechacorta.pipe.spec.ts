import { FechacortaPipe } from './fechacorta.pipe';

describe('FechacortaPipe', () => {
  it('create an instance', () => {
    const pipe = new FechacortaPipe();
    expect(pipe).toBeTruthy();
  });
});
